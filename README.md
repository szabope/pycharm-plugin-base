# Common code for integrating Python code quality tools into Intellij Platform IDE

This is not a plugin itself, but all kinds of generalized code for mypy and pylint integration plugins.

PythonCore is required.

### I also wouldn't fork this.
